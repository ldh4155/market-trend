# AI Agent Rules

이 프로젝트에 접근하는 모든 AI 에이전트는 아래 규칙을 반드시 준수해야 한다.

---

## 1. Git 작업 금지

`git commit`, `git push`, `git pull` 을 포함한 모든 git 이력 변경 및 원격 동기화 작업을 절대 수행하지 않는다.

로컬 파일 편집, 읽기, 테스트 실행은 허용되지만, 커밋·푸시·풀은 사람이 직접 판단하고 실행한다.

---

## 2. 수정 전 계획 먼저 작성

코드나 파일을 수정하기 전에 반드시 다음 내용을 먼저 텍스트로 작성한다.

- 무엇을 왜 수정하는지
- 어떤 파일의 어떤 부분을 어떻게 바꿀 것인지
- 예상되는 영향 범위

사용자의 명시적 동의(또는 계속 진행 신호) 이후에 실제 수정을 수행한다.

---

## 3. 코드 생성 후 반드시 테스트 & 결과 보고

코드를 생성하거나 수정한 후에는 반드시 테스트를 실행하고, 그 결과(출력, 오류, 통과 여부)를 사용자에게 그대로 보여준다.

- 테스트가 불가능한 환경이라면 그 이유를 명시한다.
- "아마 잘 될 것 같습니다" 같은 추측 보고는 허용되지 않는다.

---

## 4. 민감한 값은 반드시 .env에 보관하고 config를 통해 주입

API 키, DB URL, 아이디, 비밀번호, 시크릿 토큰 등 민감한 값은 코드에 직접 작성하지 않는다.

- 모든 민감한 값은 `.env` 파일에 보관한다.
- 코드에서는 `config` 모듈(예: `app/core/config.py`)을 통해서만 접근한다.
- `.env` 파일은 `.gitignore`에 포함되어야 하며, 절대 커밋하지 않는다.
- AI 에이전트도 민감한 값을 코드나 응답 텍스트에 하드코딩해서는 안 된다.
